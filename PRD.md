# 我的工作生活助手 — 产品需求文档（PRD）

> 版本：第二版（v2）  
> 最后更新：2026-09-14  
> 状态：已确认，作为后续开发依据

---

## 1. 产品目标和使用场景

### 目标
做一个只属于自己一个人的工作生活管理 App，把日常的计划、自媒体运营、冥想、日记、习惯、健身、饮食、游戏娱乐等场景集中在一个界面里管理。

### 使用场景
- 只在**自己的电脑浏览器**中本地运行，双击网页文件即可打开。
- 不需要登录、不需要联网、不需要云数据库、不需要部署、不需要手机端。
- 数据保存在当前电脑的浏览器本地存储中，刷新、关闭浏览器、重启电脑后数据不丢失。
- 固定使用同一个浏览器（推荐 Edge 或 Chrome）访问。

### 技术形态
- 单文件 HTML（内含 CSS + JS），双击用浏览器打开即可使用。
- 数据存储：浏览器 `localStorage`，键名 `lifeapp_v1`。
- 第一版只存文字和数字，不存照片/图片。

---

## 2. 整体页面框架和导航结构

### 布局
- 左侧固定侧边导航栏，右侧主内容区。
- 侧边导航包含 9 个入口，对应 9 个模块。

### 导航结构
| 序号 | 模块名称 | 图标 |
|------|----------|------|
| 1 | 首页总览 | 🏠 |
| 2 | 今日计划 | 📝 |
| 3 | 自媒体 | 📱 |
| 4 | 冥想记录 | 🧘 |
| 5 | 日记习惯 | 📔 |
| 6 | 健身计划 | 💪 |
| 7 | 饮食计划 | 🍚 |
| 8 | 游戏娱乐 | 🎮 |
| 9 | 数据与设置 | ⚙️ |

### 全局交互
- 顶部标题栏显示当前模块名称，首页额外显示完整日期。
- 统一的弹窗表单（新增/编辑数据）、Toast 轻提示、删除二次确认。
- 支持深色/浅色主题切换。
- 所有数据操作即时保存到 localStorage。

---

## 3. 首页的内容结构

首页（首页总览）是信息聚合页，不产生独立数据，展示内容如下：

### 3.1 问候卡片
- 根据当前时间显示问候语（早上好/中午好/下午好/晚上好）。
- 若设置中填了称呼，问候语带称呼。
- 显示完整日期（年月日 + 星期几）。

### 3.2 模块摘要卡（8 张）
每张卡片可点击跳转到对应模块：

| 卡片 | 显示内容 |
|------|----------|
| 冥想 | 今日冥想分钟数或"今日未冥想" |
| 习惯打卡 | 今日已打卡数/总习惯数 |
| 今日训练 | 周计划中今天安排的训练内容 |
| 今日热量 | 已摄入/目标千卡，超标标红 |
| 喝水 | 今日已喝杯数 |
| 自媒体 | 待发布条数 · 已发布条数 |
| 今日娱乐 | 今日娱乐分钟数，超上限标红并提示 |
| 连续冥想 | 冥想连续天数 |

### 3.3 今日计划摘要
- 显示今日任务列表（最多前 6 条）。
- 可直接勾选/取消勾选任务完成状态。
- 显示完成进度（已完成/总数）和进度条。
- "全部"按钮跳转到今日计划模块。

### 3.4 快速备忘
- 一个输入框 + "记下"按钮。
- 回车也可快速保存。
- 保存后显示内容 + 日期时间，按时间倒序，最多显示最近 4 条。

---

## 4. 每个模块的用途

### 4.1 首页总览
聚合展示各模块今日摘要和快速备忘，是每日打开后的第一屏。

### 4.2 今日计划
按日期管理当日任务，支持时间段、优先级、备注，可切换查看历史日期。

### 4.3 自媒体
管理自媒体内容创作全流程：选题灵感 → 草稿 → 待发布 → 已发布，记录发布后的数据。

### 4.4 冥想记录
内置计时器辅助冥想，记录每次冥想的时长和感受，统计累计和连续天数。

### 4.5 日记习惯
两部分：每日文字日记（带心情标记）+ 自定义习惯每日打卡（带连续天数）。

### 4.6 健身计划
管理每周训练安排、记录每次训练的动作细节（组数/次数/重量）、跟踪体重变化趋势。

### 4.7 饮食计划
管理个人食物库（名称+热量），按日记录三餐并自动汇总热量，对比每日目标；喝水打卡。

### 4.8 游戏娱乐
管理游戏清单（想玩/在玩/已通关），记录每次娱乐时长，统计今日/本周时长，超上限时醒目提示。

### 4.9 数据与设置
数据备份（导出/导入）、清空数据、个性化设置（称呼、周起始日、热量目标、娱乐上限、主题）。

---

## 5. 每个模块第一版最必要的数据和字段

### 5.1 首页总览
- 快速备忘：`{ id, text, date(YYYY-MM-DD), timestamp }`

### 5.2 今日计划
- 任务（按日期键存储）：`{ id, text, time(HH:MM), priority(高/中/低), note, done(boolean) }`

### 5.3 自媒体
- 选题灵感：`{ id, text, platform, date }`
- 内容流水线条目：`{ id, title, platform, status(idea/draft/ready/published), url, views, likes, comments, date }`

### 5.4 冥想记录
- 记录：`{ id, date, minutes, note }`
- 统计字段（计算得出）：累计次数、累计分钟、连续天数

### 5.5 日记习惯
- 日记（按日期键存储）：`{ text, mood(开心/平静/充实/疲惫/焦虑/低落) }`
- 习惯：`{ id, name, checks[](日期数组) }`
- 统计字段（计算得出）：连续天数

### 5.6 健身计划
- 周计划：`{ 周一~周日(0-6): 训练内容文本 }`
- 训练记录：`{ id, date, name, ex[{ name, sets, reps, weight }] }`
- 体重记录：`{ id, date, kg }`

### 5.7 饮食计划
- 食物库：`{ id, name, kcal(每份热量) }`
- 每日餐饮（按日期键存储）：`{ id, name, kcal, n(份数) }`
- 喝水（按日期键存储）：`杯数(number)`
- 设置项：每日热量目标

### 5.8 游戏娱乐
- 游戏清单：`{ id, name, status(backlog/playing/done), rating(0-5), note }`
- 娱乐时长记录：`{ id, gameId, date, minutes }`
- 设置项：每日娱乐时长上限

### 5.9 数据与设置
- 设置：`{ name, weekStart(0或1), calorieGoal, entLimit, theme(light/dark) }`
- 导出：整个 state 序列化为 JSON 文件
- 导入：从 JSON 文件恢复整个 state

---

## 6. 首页、今日计划和各模块之间的关系

```
首页总览
 ├── 读取 → 今日计划（今日任务及完成进度）
 ├── 读取 → 冥想记录（今日冥想分钟数、连续天数）
 ├── 读取 → 日记习惯（今日习惯打卡进度）
 ├── 读取 → 健身计划（周计划中今日训练内容）
 ├── 读取 → 饮食计划（今日热量合计、喝水杯数）
 ├── 读取 → 自媒体（待发布数、已发布数）
 ├── 读取 → 游戏娱乐（今日娱乐时长）
 ├── 写入 → 快速备忘（独立数据，首页直接写入）
 └── 点击摘要卡 → 跳转对应模块

今日计划
 └── 数据源：按日期独立管理，首页只读取今日数据

各模块
 └── 各自独立管理数据，首页只做只读摘要展示
```

**关键关系说明：**
- 首页是所有模块的"今日摘要聚合页"，不产生独立业务数据（快速备忘除外）。
- 今日计划以日期为键，首页只读取当天数据。
- 各模块数据相互独立，互不依赖。
- 首页的摘要卡可点击跳转到对应模块。

---

## 7. 本地数据文件、备份和恢复要求

### 存储方式
- 数据存储在浏览器 `localStorage`，键名 `lifeapp_v1`，值为 JSON 字符串。
- 整个 state 结构版本号 `v: 1`，用于未来兼容性判断。

### 备份
- 设置页提供"导出备份文件"功能。
- 导出格式：JSON 文件，文件名形如 `生活助手备份-YYYY-MM-DD.json`。
- 导出内容：完整 state 数据。
- 建议用户不定期手动导出一份保存到电脑文件夹。

### 恢复
- 设置页提供"从备份文件导入"功能。
- 导入时二次确认（导入将覆盖当前全部数据）。
- 导入的文件必须是 `v: 1` 格式，否则提示格式不正确。
- 导入成功后立即刷新界面和主题。

### 清空
- 设置页提供"清空全部数据"功能。
- 需两次确认，防止误操作。
- 清空后恢复为默认初始状态（包含 8 种预置食物）。

### 数据安全提醒
- localStorage 中的数据在手动清理浏览器数据/缓存时会丢失。
- 重装浏览器、换浏览器也会导致数据丢失。
- **清理浏览器数据前务必先导出备份。**

---

## 8. 第一版必须完成的功能

### 首页
- [x] 问候语 + 完整日期
- [x] 8 张模块摘要卡（可点击跳转）
- [x] 今日计划摘要（前 6 条 + 进度条 + 可勾选）
- [x] 快速备忘（输入 + 回车保存 + 列表展示最近 4 条）

### 今日计划
- [x] 按日期添加/编辑/删除任务
- [x] 时间段、优先级（高/中/低）、备注
- [x] 勾选完成
- [x] 日期前后切换 + 回到今天
- [x] 完成进度显示

### 自媒体
- [x] 选题灵感库（添加/删除/转入内容）
- [x] 内容流水线看板（选题→草稿→待发布→已发布）
- [x] 新建/编辑/删除内容
- [x] 状态推进（←/→）
- [x] 已发布内容可填链接和浏览/点赞/评论数据

### 冥想记录
- [x] 计时器（开始/结束/放弃）
- [x] 结束后自动生成记录（时长 + 备注）
- [x] 手动补记
- [x] 统计：累计次数、累计分钟、连续天数
- [x] 最近记录列表

### 日记习惯
- [x] 日记：每日一篇，心情标记，自动保存
- [x] 日期前后切换 + 回到今天
- [x] 习惯：自定义添加/删除
- [x] 每日打卡（今日 + 最近 7 天补打卡）
- [x] 连续天数显示

### 健身计划
- [x] 周计划（周一~周日训练安排，自动保存）
- [x] 训练记录（日期 + 内容 + 动作表格）
- [x] 动作：名称/组数/次数/重量
- [x] 体重记录 + 折线图
- [x] 历史记录列表

### 饮食计划
- [x] 食物库管理（预置 8 种 + 增删）
- [x] 每日三餐记录（从食物库选 + 自定义）
- [x] 按份数计算热量，自动汇总当日合计
- [x] 对比每日热量目标，超标标红
- [x] 喝水打卡（8 杯目标）

### 游戏娱乐
- [x] 游戏清单（想玩/在玩/已通关，可增删）
- [x] 星级评分 + 感想备注
- [x] 状态下拉切换
- [x] 娱乐时长记录（游戏 + 日期 + 分钟）
- [x] 今日/本周时长统计
- [x] 超上限醒目提示（不强制阻止）

### 数据与设置
- [x] 称呼、周起始日、热量目标、娱乐上限、深色/浅色主题
- [x] 导出备份文件
- [x] 从备份文件导入
- [x] 清空全部数据（二次确认）

### 全局
- [x] 刷新/关闭/重启后数据不丢失
- [x] 统一弹窗表单、Toast 提示、删除确认
- [x] 深色/浅色主题

---

## 9. 第一版暂时不做的功能

- 手机端、平板适配（只保证电脑浏览器好用）
- 联网、云同步、多设备同步
- 账号登录、部署上线
- 照片/图片/附件上传
- 浏览器关闭后的系统通知提醒（页面内提示可以有）
- 复杂图表和深度数据分析（只做简单数字汇总和小折线图）
- AI 功能
- 富文本排版（日记用纯文本多行输入）
- 密码保护/加密
- 多用户/权限管理
- 自动定时备份

---

## 10. 可以实际检查的验收标准

### 启动与基础
1. 双击 `index.html`，浏览器正常打开，无 JS 报错。
2. 左侧显示 9 个导航按钮，点击可切换模块。
3. 首页显示问候语、日期、8 张摘要卡、今日计划摘要、快速备忘输入框。

### 数据持久化（核心验收项）
4. 在任意模块添加数据后，刷新页面（F5），数据仍然存在。
5. 关闭浏览器标签页后重新打开文件，数据仍然存在。
6. 重启电脑后重新打开文件，数据仍然存在。

### 首页
7. 快速备忘输入文字按回车，备忘出现在列表中，输入框清空。
8. 摘要卡数字与各模块实际数据一致（如冥想 15 分钟、热量 348 千卡等）。
9. 点击摘要卡跳转到对应模块。

### 今日计划
10. 可添加任务（含时间段、优先级、备注），勾选后状态保存。
11. 切换日期可查看历史，新日期为空白。

### 自媒体
12. 选题灵感添加后可"转入内容"，出现在流水线"草稿"列。
13. 内容可通过 ←/→ 按钮在选题→草稿→待发布→已发布之间推进。
14. 已发布内容可填入链接和数据。

### 冥想记录
15. 计时器开始后实时计时，结束后弹窗记录时长和备注。
16. 手动补记可指定日期。
17. 累计次数、累计分钟、连续天数正确。

### 日记习惯
18. 日记输入后停顿片刻自动保存（无需手动点保存）。
19. 习惯添加后今日自动打卡，连续天数为 1。
20. 点击最近 7 天的圆点可补/取消打卡，连续天数正确更新。

### 健身计划
21. 周计划修改后自动保存。
22. 训练记录可添加动作（名称/组数/次数/重量），表格正确显示。
23. 体重添加两条以上后，折线图正常渲染。

### 饮食计划
24. 从食物库选择食物 + 份数，当日热量合计正确计算。
25. 热量超过目标值时数字标红。
26. 喝水 +/- 可调整杯数，已喝水杯高亮。

### 游戏娱乐
27. 游戏可在想玩/在玩/已通关之间切换。
28. 记录娱乐时长后，今日/本周统计正确。
29. 超过每日上限时显示醒目提示。

### 数据与设置
30. 修改称呼后首页问候语包含该称呼。
31. 切换深色主题后全站立即变为深色。
32. 导出备份文件可下载到电脑。
33. 导入备份文件后数据恢复。
34. 清空数据需二次确认，清空后恢复初始状态（含 8 种预置食物）。

### 弹窗与交互
35. 所有弹窗表单的下拉框（优先级、平台、食物选择等）能正确提交选中值。
36. 必填项为空时提交有提示，不产生空数据。
37. 删除操作有确认提示。
38. 操作成功后有 Toast 提示。

---

## 11. 第二版新增：数据分析视图

> v2 在 v1 全部功能基础上新增 5 项数据分析视图，数据版本号升至 `v: 2`，自动兼容 v1 数据迁移。

### 11.1 习惯月度热力图
- 在「日记习惯 → 习惯打卡」页面底部新增 GitHub 风格月度打卡热力图。
- 按完成比例分 5 级颜色（l0-l4），周一开头，显示日期格 + 星期表头。
- 支持月份前后翻页（跨年正确）和「本月」快捷回到当月。
- 底部统计：本月有打卡的日期天数、全部习惯完成的天数、习惯总数。
- 无习惯时显示引导提示。

### 11.2 饮食热量周趋势
- 在「饮食计划」页面新增第 3 个子页签「📈 热量趋势」。
- 展示近 7 天每日摄入热量折线图，含红色目标虚线（值为设置的热量目标）。
- 超过目标的数据点标红。
- 统计卡片：近 7 天合计、有记录日均、超目标天数。
- 修改热量目标后目标线和红点实时更新。

### 11.3 冥想时长近 30 天趋势
- 在「冥想记录」页面统计卡片下方新增近 30 天冥想时长折线图。
- 30 天数据聚合同一天的记录，折线含 30 个数据点。
- 统计：近 30 天共冥想多少分钟、多少天有练习。
- 无记录时显示 30 天平线（非空状态）。

### 11.4 娱乐时长周报告
- 在「游戏娱乐」页面新增第 3 个子页签「📊 本周报告」。
- 本周每天娱乐时长柱状图（7 根柱子），有娱乐的天数显示数值。
- 统计卡片：本周总时长、有娱乐天数、环比上周增减（分钟 + 百分比）。
- 环比文案覆盖：比上周多/少 N 分钟（±N%）、两周都无记录、上周无记录本周有、与上周持平。

### 11.5 自媒体发布数据看板
- 在「自媒体」页面新增第 3 个子页签「📊 数据看板」。
- 仅统计 `status==='published'` 的内容，草稿/待发布等不计入。
- 按平台汇总表格：平台名、已发布篇数、浏览量、点赞、评论、互动率。
- 总计行 + 4 张统计卡片（已发布内容数、总浏览、总点赞、总评论含互动率）。
- 单篇数据 Top 5 排行（按浏览量降序）。
- 无已发布内容时显示空状态引导。浏览为 0 时互动率显示 0.0% 而非 NaN。

### v2 验收标准（追加）

39. 习惯热力图按完成比例正确分色（l0-l4），可跨月翻页回到本月。
40. 饮食热量趋势折线含 7 个数据点和红色目标虚线，超目标数据点标红。
41. 冥想近 30 天趋势聚合同日记录，折线含 30 个数据点。
42. 娱乐周报告柱状图 7 根柱子，环比上周文案和百分比正确。
43. 自媒体看板按平台汇总只统计已发布内容，互动率不出现 NaN。

## 12. 第三版新增：本地能力扩展（阶段五可选扩展）

> v3 不改变备份版本号（仍为 `v: 2`，旧数据自动补齐新字段）。围绕「图片、提醒、隐私、表格、可安装」补齐五项本地能力；数据仍全部保存在本机。

### 12.1 日记配图（IndexedDB）
- 日记每条可添加多张照片，图片以 Blob 存入 IndexedDB（库 `lifeapp_imgs`，store `images`），不占 localStorage 空间。
- 日记条目结构升级为 `{text,mood,photos:[id]}`；日记页缩略图可单张删除；只有图片没有文字的条目不会被自动保存误删。
- 选图限制：仅图片、单张不超过 8MB；非图片/超限自动跳过并提示数量。
- 导出备份时图片打包为 `_images`（id→dataURL 映射，整体备份上限 50MB），不进 localStorage；导入时先清空再逐张还原，损坏图片计入失败数但不影响文本恢复。
- 清空全部数据同步清空图片库。

### 12.2 备份提醒（Notification + 页内降级）
- 设置项：备份提醒开关（默认开启）、间隔天数（默认 14 天，最小 1）。
- 基准日期：上次导出时间，从未导出则按首次使用日期；超期时首页显示提示条，可「立即导出」或「今天不再提示」（次日自动恢复）。
- 已授权系统通知时，启动检查每天最多推送一条系统通知；未授权/不支持 Notification 时不报错，页内提示条照常工作。
- 成功导出备份后自动记录导出日期，提示条消失。

### 12.3 PIN 锁屏（纯前端本地校验）
- 设置页可开启 PIN 锁并设置 4-6 位数字 PIN；PIN 仅保存在本机浏览器，用于防止身边人随手翻看（明确不承诺高强度安全）。
- 开启后每次打开应用先显示锁屏遮罩：PIN 错误提示并抖动、清空重输，支持回车解锁。
- 锁屏期间拦截一切导航与操作（不仅靠遮罩遮挡，事件层同样拦截）；关闭锁屏时同时清除 PIN。
- 当前会话开启后下次打开才锁屏，不打断正在使用的用户。

### 12.4 各模块 CSV 导出
- 设置 → 数据备份卡片提供 9 个模块的 CSV 导出：计划、饮食、饮水、娱乐、训练、体重、冥想、日记、习惯。
- 统一 RFC 4180 转义（逗号/引号/换行）、CRLF 行分隔、UTF-8 BOM（Excel 打开中文不乱码）。
- 各模块列设计：
  - 计划：日期/内容/计划时间/优先级/备注/状态
  - 饮食：日期/餐次/食物/份数/热量；饮水：日期/杯数（0 杯日期不导出）
  - 娱乐：日期/游戏名（已删除游戏显示占位）/时长
  - 训练：日期/训练名/动作/组数/次数/重量（多动作展开多行）；体重：日期/体重
  - 冥想：日期/时长/备注；日记：日期/心情/文字/配图数；习惯：习惯/累计天数/当前连续天数
- 空数据导出仅含表头；导出后提示条数。

### 12.5 PWA（可安装、可离线）
- 新增 `manifest.webmanifest`（standalone 显示模式、主题色、应用名）和 SVG 应用图标 `icon.svg`。
- 新增 `sw.js`：安装预缓存应用外壳（index.html / manifest / icon）；页面导航网络优先（永远先拿最新版，离线回退缓存），静态资源缓存优先并后台更新；激活时清理旧缓存。
- 仅在 http(s) 安全上下文注册 Service Worker，`file://` 双击打开时静默跳过，原有使用方式不受任何影响。

### v3 验收标准（追加）

44. 日记可添加/删除多张配图，刷新后仍在；非图片与超 8MB 图片被跳过；仅图片的日记不会丢失。
45. 备份 JSON 中包含 dataURL 图片映射且 localStorage 中不含图片数据；50MB 以上备份文件被拒绝导入。
46. 导入带图片备份后图片可还原回 IndexedDB，损坏图片只计入失败提示。
47. 超过间隔未备份时首页出现提示条；导出后消失；「今天不再提示」当日隐藏、次日恢复。
48. 已授权系统通知时超期启动每天至多一条通知；不支持/未授权时不报错。
49. 开启 PIN 后重新打开应用必须解锁；锁屏期间无法切换页面或写入数据；错误 PIN 不解锁并提示；关闭后 PIN 被清除。
50. 9 个模块均可导出 CSV，特殊字符（逗号/引号/换行）正确转义，文件带 UTF-8 BOM。
51. manifest 与 sw.js 可访问且字段完整；http 环境下 SW 注册激活、重新加载后受其控制且应用外壳已缓存；`file://` 下不注册。

### 自动化测试
- 共 **110 项**自动化测试全部通过（v1/v2 共 66 项；v3：配图 9 项、备份提醒 11 项、PIN 锁 8 项、CSV 10 项、PWA 6 项）。
- 测试基建：`tests/runner.html` 支持 `?grp=` 分组运行；为适配失焦窗口定时器节流，短等待改用 MessageChannel 宏任务滴答（`mtick`），防抖/真实计时等待使用墙钟等待（`rsleep`）。
